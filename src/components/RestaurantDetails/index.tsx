import React from 'react';

import test from '../../gql/queries/users.graphql';

const RestaurantDetails = ({

}) => {
  console.log('does this work', test);
  return (
    <span>wefwefewwef</span>
  )
};

export default RestaurantDetails;
