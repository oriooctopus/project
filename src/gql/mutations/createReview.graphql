mutation createReview(
  $content: String!
  $rating: Int!
  $restaurantId: Int!
) {
  addReview(
    input: {
      content: $content
      rating: $rating
      restaurantId: $restaurantId
    }
  ) {
    id
  }
}
